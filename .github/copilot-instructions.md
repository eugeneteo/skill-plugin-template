# {{PLUGIN_NAME}} — GitHub Copilot instructions

@../AGENTS.md

GitHub Copilot CLI, the cloud agent, and code review already discover
`AGENTS.md` natively, but Copilot Chat on GitHub.com only reads this
file, so it stays present and imports `AGENTS.md` above via the `@`
syntax to avoid drift.
