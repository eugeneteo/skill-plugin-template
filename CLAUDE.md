# {{PLUGIN_NAME}} — Claude Code instructions

@AGENTS.md

Claude Code loads `AGENTS.md` above via the `@` import syntax, so this
file never drifts out of sync with the repository overview and Git
commit conventions defined there.
